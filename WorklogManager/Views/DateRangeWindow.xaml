<Window x:Class="WorklogManager.Views.DateRangeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Load from TogglTrack"
        Width="320" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <StackPanel Margin="20">

        <TextBlock Text="Select date range to import:" FontWeight="SemiBold" Margin="0,0,0,14"/>

        <DockPanel Margin="0,0,0,8">
            <Label Width="55" Content="From:" VerticalAlignment="Center"/>
            <DatePicker x:Name="FromPicker" SelectedDateChanged="DatePicker_Changed"/>
        </DockPanel>

        <DockPanel Margin="0,0,0,20">
            <Label Width="55" Content="To:" VerticalAlignment="Center"/>
            <DatePicker x:Name="ToPicker" SelectedDateChanged="DatePicker_Changed"/>
        </DockPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="LoadButton" Content="Load" IsDefault="True"
                    Click="LoadButton_Click" IsEnabled="False"
                    Padding="18,6" Margin="0,0,8,0"/>
            <Button Content="Cancel" IsCancel="True"
                    Click="CancelButton_Click" Padding="18,6"/>
        </StackPanel>

    </StackPanel>
</Window>
